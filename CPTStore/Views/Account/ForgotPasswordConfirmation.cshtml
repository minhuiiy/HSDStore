@{
    ViewData["Title"] = "Xác nhận quên mật khẩu";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0">@ViewData["Title"]</h4>
                </div>
                <div class="card-body p-4">
                    <div class="text-center mb-4">
                        <i class="bi bi-envelope-check" style="font-size: 3rem; color: #28a745;"></i>
                    </div>
                    <h5 class="text-center mb-3">Email đã được gửi</h5>
                    <p class="text-center mb-4">
                        Chúng tôi đã gửi một email chứa liên kết đặt lại mật khẩu đến địa chỉ email của bạn.
                        Vui lòng kiểm tra hộp thư đến (và thư mục spam nếu cần) để tiếp tục quá trình đặt lại mật khẩu.
                    </p>
                    <div class="text-center">
                        <a asp-action="Login" class="btn btn-primary">Quay lại đăng nhập</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>